# Prompt packages

Mỗi use case sẽ có prompt và output schema riêng: `tutor`, `summary`, `flashcards`, `quiz`.

Prompt phải được version cùng với model, parser, chunker và schema. Không dùng lại system prompt của research agent cũ.
